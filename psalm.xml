<?xml version="1.0"?>
<psalm
    errorLevel="1"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="https://getpsalm.org/schema/config"
    xsi:schemaLocation="https://getpsalm.org/schema/config vendor/vimeo/psalm/config.xsd"
    findUnusedBaselineEntry="true"
    findUnusedCode="false"
    cacheDirectory=".psalm/cache"
>
    <projectFiles>
        <directory name="src"/>
        <directory name="tests"/>
        <ignoreFiles>
            <directory name="vendor"/>
            <!-- Recipe code, and the skeleton ships the kernel non-final. -->
            <file name="src/Kernel.php"/>
            <file name="tests/bootstrap.php"/>
            <!-- Foundry's factories resolve against ObjectFactory<object>, so every
                 initialize() and with*() draws MoreSpecificReturnType and friends.
                 Suppressing them per method meant a docblock on each one. -->
            <directory name="src/Factory"/>
        </ignoreFiles>
    </projectFiles>

    <forbiddenFunctions>
        <function name="var_dump" />
        <function name="dd" />
        <function name="dump" />
    </forbiddenFunctions>

    <plugins>
        <pluginClass class="Psalm\SymfonyPsalmPlugin\Plugin"/>
        <pluginClass class="Weirdan\DoctrinePsalmPlugin\Plugin"/>
        <pluginClass class="Psalm\PhpUnitPlugin\Plugin"/>
    </plugins>
</psalm>
